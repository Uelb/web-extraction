{
  "name": "web-extraction",
  "version": "0.1.0",
  "devDependencies": {
    "grunt": "~0.4.4",
    "grunt-contrib-watch": "^0.5.3",
    "grunt-contrib-uglify": "^0.4.0",
    "grunt-contrib-coffee": "^0.10.1"
  },
  "scripts": {
    "install": "npm install -g grunt-cli && npm install -g bower"
  }
}
